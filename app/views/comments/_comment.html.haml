%li
  %span.content= comment.content
  %span.timestamp
    Posted
    = time_ago_in_words(comment.created_at)
    ago
    - if image_page
      by
      = link_to comment.user.name, comment.user
    - else
      on
      = link_to comment.image.caption, comment.image
